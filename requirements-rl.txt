# LeoBook RL Dependencies
# PyTorch CPU-only (lightweight, no CUDA)
# Install via: pip install -r requirements-rl.txt
--index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

--index-url https://pypi.org/simple/
safetensors>=0.4.0
numpy>=1.24.0
